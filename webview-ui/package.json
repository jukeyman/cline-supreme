{
  "name": "cline-supreme-webview",
  "version": "2.0.0",
  "private": true,
  "description": "Webview UI for Cline Supreme",
  "scripts": {
    "build": "npm run copy-assets",
    "dev": "npm run copy-assets && npm run watch",
    "watch": "npm run copy-assets",
    "copy-assets": "mkdir -p dist && cp -r ../src/webview/* dist/",
    "clean": "rm -rf dist",
    "lint": "echo 'Linting webview files...'",
    "format": "echo 'Formatting webview files...'",
    "test": "echo 'Testing webview...'"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}